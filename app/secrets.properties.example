# Local secrets — copy to secrets.properties and fill in.
# secrets.properties is git-ignored; secrets.properties.example is tracked.
#
# Get a key at https://aistudio.google.com/apikey — free tier is enough for team use.
GEMINI_API_KEY=your_gemini_api_key_here
